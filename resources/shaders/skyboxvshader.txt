#version 310 es
layout (location = 0) in mediump vec3 aPos;

out mediump vec3 TexCoords;

uniform mat4 projection;
uniform mat4 view;

void main()
{
    TexCoords = aPos;
    mediump vec4 pos = projection * view * vec4(aPos, 1.0);
    gl_Position = pos.xyww;
}  